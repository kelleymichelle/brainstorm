<div name="top-title">
<h1>All Current Ideas</h1>
</div>
<div name="about-accountable">
<% @ideas.each do |idea| %>
  <%= link_to idea.title, idea_path(idea) %> -
  <%= idea.description %> -
  <%= link_to idea.inventor.name, inventor_path(idea.inventor) %>
<% end %> 
</div> 
